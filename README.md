# Weekly Radar

Volume & Momentum Scanner Dashboard. Deployed to GitHub Pages.

## Update

```bash
cd /Users/Chikara/.openclaw/workspace
python3 scripts/scanner_dashboard.py
cd weeklyradar && git add -A && git commit -m "update data" && git push
```

Or use: `scripts/update_scanner_dashboard.sh`
